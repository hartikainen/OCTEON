# Building

For simple-exec:
   $ make

For Linux:
   $ make OCTEON_TARGET=linux_64

# Running

On simple-exec (for 32 cores):
   # tftp $loadaddr passthrough; bootoct $loadaddr coremask=ffffffff

On Linux:
   # ./passthrough-linux_64