# Building

For simple-exec:
   $ make

For Linux:
   $ make OCTEON_TARGET=linux_64

# Running

On simple-exec (for 32 cores):
   # tftp $loadaddr forward; bootoct $loadaddr coremask=ffffffff

On Linux:
   # ./forward-linux_64